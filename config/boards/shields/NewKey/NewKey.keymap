#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
        bindings = <
           &kp A &kp B &kp C_MUTE
    >;
            sensor-bindings = <&encoder_cw C_VOL_UP &encoder_ccw C_VOL_DN>;
        };
    };
};
